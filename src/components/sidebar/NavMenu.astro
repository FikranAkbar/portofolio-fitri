---
const navItems = [
  { href: '#',           label: 'Home',            active: true  },
  { href: '#projects',   label: 'Project',         active: false },
  { href: '#about',      label: 'About Me',        active: false },
  { href: '#resume',     label: 'Resume',          active: false },
  { href: '#gallery',    label: 'Visitor Gallery', active: false },
];
---

<nav aria-label="Explore navigation">
  <p class="text-[10px] font-semibold tracking-widest text-gray-400 uppercase mb-2 px-3">
    Explore
  </p>
  <div class="flex flex-col gap-0.5">
    {navItems.map(item => (
      <a
        href={item.href}
        class:list={[
          "nav-item group flex items-center justify-between px-3 py-2 rounded-lg text-sm transition-all duration-200",
          item.active
            ? "bg-gray-100 text-gray-900 font-medium"
            : "text-gray-500 hover:bg-gray-50 hover:text-gray-800"
        ]}
      >
        <span>{item.label}</span>
        <span class="arrow-icon text-gray-400" aria-hidden="true">→</span>
      </a>
    ))}
  </div>
</nav>

